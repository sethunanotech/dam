using DAM.Domain.InfraEntities;

namespace DAM.Application.Contracts
{
    public interface IStorage : IDisposable
    {
        //Task<IReadOnlyCollection<Blob>> ListAsync(ListOptions options = null);
    }
}
